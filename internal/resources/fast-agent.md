# Fast-Agent

`pip install fast-agent-mcp` install fast-agent ^run
`uv pip install fast-agent-mcp` install with uv ^run
`fast-agent init {{name}}` create new project ^run:name
`fast-agent run` run agent ^run
`fast-agent chat` interactive chat mode ^run
`fast-agent setup` configure LLM providers ^run
